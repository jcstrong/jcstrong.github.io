---
title: "2.4 用户管理"
category: skills
folderPath: "Linux/Linux命令"
folderTop: "Linux"
tags: ["Linux", "运维"]
featured: false
source: "Linux/Linux命令/2.4 用户管理.md"
updated: "2022-06-06"
readingTime: 2
summary: "toc  文件拥有者\towner  用户组\tgroup  其他用户\tothers  超级用户\troot  用户与用户组管理文件 在 linux 中，用户帐号，用户密码，用户组信息和用户组密码均是存放在不同的/etc/配置文件中的。 在 li..."
---
[toc]

> **文件拥有者	owner**
>
> **用户组	group**
>
> **其他用户	others**
>
> **超级用户	root**

### 用户与用户组管理文件

在 linux 中，用户帐号，用户密码，用户组信息和用户组密码均是存放在不同的/etc/配置文件中的。

在 linux 系统中，所创建的用户帐号和其相关信息 (密码除外) 均是存放在 / etc/passwd 配置文件中。

由于所有用户对 passwd 文件均有读取的权限，因此密码信息并未保存在该文件中，而是保存在了 / etc/shadow 的配置文件中。
若要使某个用户账户不能登录 linux，只需设置该用户所使用的 shell 为 / sbin/nologin 即可。比如，对于 FTP 账户，一般只允许登录和访问 FTP 服务器，不允许登录 linux 操作系统。若要让某用户没有 telnet 权限，即不允许该用户利用 telnet 远程登录和访问 linux 操作系统，则设置该用户所使用的 shell 为 / bin/true 即可。若要让用户没有 telnet 和 ftp 登录权限，则可设置该用户的 shell 为 / bin/false。

#### passwd配置文件

保存用户帐号和其相关信息 (密码除外) 

```
[root@localhost test]# cat /etc/passwd
```

这个文件每一行代表一个用户，大多数都是系统用户

```
chenjun:x:1000:1000:xCentos:/home/chenjun:/bin/bash
```

用户账号的名称：密码占位符x：用户账号的UID：组账号的GID：用户全名：用户的home目录：默认的shell

UID：普通用户：1000～65535；系统用户：1～999；root用户：0；

#### shadow 配置文件

为安全起见，用户真实的密码采用 MD5 加密算法加密后，保存在 / etc/shadow 配置文件中，该文件只有 root 用户可以读取。

与 passwd 文件类似，shadow 文件也是每行定义和保存一个账户的相关信息。第一个字段为用户帐户名，第二个字段为账户的密码。![截屏2021-02-09 下午2.59.20](https://jcstrong.github.io/img/20210209150115.png)

#### group配置文件

```
chenjun:x:1000:
组名：密码：GID：组内用户列表
```

##### groupadd命令

创建新组

##### gpasswd命令

设置组账号密码

```
[root@localhost test]# useradd test
[root@localhost test]# cat /etc/passwd | grep test
test:x:1001:1001::/home/test:/bin/bash
[root@localhost test]# groupadd testgroup
[root@localhost test]# cat /etc/group | grep test
test:x:1001:
testgroup:x:1002:
[root@localhost test]# usermod -a -G testgroup test
[root@localhost test]# groups test
test : test testgroup
```



