---
title: "2.2.2 cd 命令"
category: skills
folderPath: "Linux/Linux命令"
folderTop: "Linux"
tags: ["Linux", "运维"]
featured: false
source: "Linux/Linux命令/2.2.2 cd 命令.md"
updated: "2021-02-09"
readingTime: 3
summary: "toc Linux cd 命令可以说是Linux中最基本的命令语句，其他的命令语句要进行操作，都是建立在使用 cd 命令上的 所以，学习Linux 常用命令，首先就要学好 cd 命令的使用方法技巧。  命令格式： cd 目录名  命令功能：..."
---
[toc]

Linux cd 命令可以说是Linux中最基本的命令语句，其他的命令语句要进行操作，都是建立在使用 cd 命令上的

所以，学习Linux 常用命令，首先就要学好 cd 命令的使用方法技巧。

## 命令格式：

cd [目录名]

## 命令功能：

切换当前目录至dirName

## 常用范例

### 实例1：进入系统根目录

#### 命令：cd /

输出：
[root@localhost ~]# cd /
进入系统根目录,上面命令执行完后拿ls命令看一下，当前目录已经到系统根目录了

#### 命令：cd … 或者 cd … //	使用“ cd … ”一直退，就可以到达根目录

输出:

```
[root@localhost soft]# pwd
/opt/soft
[root@localhost soft]# cd …
[root@localhost opt]# cd …//
[root@localhost /]# pwd
/
```



#### 命令：cd …/… //

输出：

```
[root@localhost soft]# pwd
/opt/soft
[root@localhost soft]# cd …/… //
[root@localhost /]# pwd
/
[root@localhost /]#
```

使用cd 命令实现进入当前目录的父目录的父目录。

使用cd 命令实现进入当前目录的父目录的父目录。

### 实例2：使用 cd 命令进入当前用户主目录

“当前用户主目录”和“系统根目录”是两个不同的概念。进入当前用户主目录有两个方法。

#### 命令：cd

输出：

```
[root@localhost soft]# pwd
/opt/soft
[root@localhost soft]# cd
[root@localhost ~]# pwd
/root
```

#### 命令2：cd ~

输出：

```
[root@localhost soft]# pwd
/opt/soft
[root@localhost soft]# cd ~
[root@localhost ~]# pwd
/root
```



### 实例3：跳转到指定目录

#### 命令：cd /opt/soft

输出：

```
[root@localhost soft]# pwd
/opt/soft
[root@localhost soft]# cd jdk1.6.0_16/
[root@localhost jdk1.6.0_16]# pwd
/opt/soft/jdk1.6.0_16
[root@localhost jdk1.6.0_16]#
```

跳转到指定目录，从根目录开始，目录名称前加 / ,当前目录内的子目录直接写名称即可

### 实例4： 进入此目录之前所在目录

#### 命令：cd -

输出：

```
[root@localhost soft]# cd /usr/bin
[root@localhost bin]# cd /home/jun/test
[root@localhost test]# cd -
/usr/bin
[root@localhost jdk1.6.0_16]#
```

### 实例3： 把上个命令的参数作为cd参数

#### 命令：cd !$

输出：

```
[root@localhost soft]# cd /usr/bin
[root@localhost bin]# cd /home/jun/test
[root@localhost test]# cd !$
/usr/bin
[root@localhost jdk1.6.0_16]#
```



## pwd ##

绝对路径方式显示用户当前工作目录

### 补充说明 ###

**pwd命令** 以绝对路径的方式显示用户当前工作目录。命令将当前目录的全路径名称（从根目录）写入标准输出。全部目录使用`/`分隔。第一个`/`表示根目录，最后一个目录是当前目录。执行pwd命令可立刻得知您目前所在的工作目录的绝对路径名称。


###  语法

	pwd（选项）

###  选项

	--help：显示帮助信息；
	--version：显示版本信息。

###  参数


###  实例

#### 显示当前目录所在路径.

```
	# pwd
	# tree -L 2
```

#### 多层连接文件时，显示所有连接文件最终指向的文件全路径

​	/root目录下面有个dir1目录，test连接文件指向dir1目录，/opt目录下面有一个test连接文件，指向/root/test连接文件，
​	通过cd命令进入/opt/test，pwd默认，只显示连接文件的全路径

```
# pwd -P
```



