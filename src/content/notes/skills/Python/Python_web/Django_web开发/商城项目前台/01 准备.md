---
title: "01 准备"
category: skills
folderPath: "Python/Python_web/Django_web开发/商城项目前台"
folderTop: "Python"
tags: ["Python", "Web"]
featured: false
source: "Python/Python_web/Django_web开发/商城项目前台/01 准备.md"
updated: 2021-09-27
readingTime: 5
summary: "toc  设计  项目主要模块 | 模块           | 功能                                             | |  |  | | 验证       | 图形验证、短信验证        ..."
---
[toc]

# 设计

### 项目主要模块

| 模块           | 功能                                             |
| -------------- | ------------------------------------------------ |
| **验证**       | 图形验证、短信验证                               |
| **用户**       | 注册、登录、用户中心                             |
| **第三方登录** | QQ登录                                           |
| **首页广告**   | 首页广告                                         |
| **商品**       | 商品列表、商品搜索、商品详情                     |
| **购物车**     | 购物车管理、购物车合并                           |
| **订单**       | 确认订单、提交订单                               |
| **支付**       | 支付宝支付、订单商品评价                         |
| **MIS系统**    | 数据统计、用户管理、权限管理、商品管理、订单管理 |



### 项目开发模式

| 选项         | 技术选型                |
| ------------ | ----------------------- |
| **开发模式** | 前后端不分离            |
| **后端框架** | Django + Jinja2模板引擎 |
| **前端框架** | Vue.js                  |

> - 前后端不分离的开发模式，是为了提高**搜索引擎排名**，即**SEO**。特别是首页，详情页和列表页。
> - 页面需要整体刷新：我们会选择使用Jinja2模板引擎来实现。
> - 页面需要局部刷新：我们会选择使用Vue.js来实现。



### 项目架构设计

- 静态服务
  	Nginx服务器
  		静态首页、商品详情页、CSS、JS、images...
- 代理服务
  	Nginx服务器
  		反向代理：代理美多商城服务器
- 动态服务
  	uwsgi服务器
  		美多商城程序：登陆注册验证购物车支付订单……
- 后端服务
  - 数据存储和缓存：MySQL + Redis
  - 异步任务：Celery + RabbitMQ	
  - 定时任务：Crontab
  - 容器化：Docker
  - 分布式存储：FastDFS
  - 全文检索：Elasticsearch
- 外部接口：发送短信：容联云、QQ互联：QQ登录、支付宝：订单支付



# 代码托管

在gitee创建空仓库

```bash
git clone https://gitee.com/……
```

pycharm打开本地文件夹

CMD + K 打开提交窗口

# 配置开发环境

- 配置文件

  - manage.py中指定了开发环境的配置信息的路径

  - wsgi.py中指定了生产环境的配置信息的路径

## jinja2模板

- 引擎：TEMPLATES	`'BACKEND': 'django.template.backends.jinja2.Jinja2',`

- 新建templates文件夹，右键-->Mark Directory as-->Template Folder，设置模版语言

- 模版文件加载路径：`'DIRS': [os.path.join(BASE_DIR, 'templates')],`

- 补充 **static 语法**用于**加载静态文件**、补充 **url 语法**用于模板中的**反向解析**

  - 新建utils包，创建Jinja2模板引擎环境配置文件

    ```python
    from jinja2 import Environment
    from django.contrib.staticfiles.storage import staticfiles_storage
    from django.urls import reverse
    
    
    def jinja2_environment(**options):
        env = Environment(**options)						# 新建环境对象env
        # 自定义语法：{{static('静态文件相对路径')}} {{url('路由的命名空间')}}
        env.globals.update({
            'static': staticfiles_storage.url,	# 获取静态文件路径前缀
            'url': reverse,											# 反向解析
        })
        return env
    
    
    """
    确保可以使用模板引擎中的{{ url('') }} {{ static('') }}这类语句 
    """
    ```

  - 编写Jinja2模板引擎环境配置代码

    `setting.py` `TEMPLATE` ` 'OPTIONS'`

    ```python
    # 补充Jinja2模板引擎环境
                'environment': 'meiduo_mall.utils.jinja2_env.jinja2_environment',
    ```

## MySQL数据库（跨机器访问）

> **1.新建MySQL数据库：meiduo_mall**

```bash
$ create database meiduo charset=utf8;
```

> **2.新建MySQL用户**

```bash
$ create user jun115 identified by '7890';
```

> **3.权限为%（表示任意ip都能连接）的远程连接用户**

```bash
$ grant all on meiduo.* to 'jun115'@'%';
```

> **4.授权结束后刷新特权**

```bash
$ flush privileges;
```

> **5.配置远程mysql`settings.py` `DATABASES = {……`**
>
> - Python3.x的版本中使用PyMySQL替代MySQLdb

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql', # 数据库引擎
        'HOST': '121.41.31.167', # 数据库主机
        'PORT': 3306, # 数据库端口
        'USER': 'jun115', # 数据库用户名
        'PASSWORD': '7890', # 数据库用户密码
        'NAME': 'meiduo' # 数据库名字
    },
}
```

- [开启防火墙、开放3306端口](https://www.cnblogs.com/dylan-fu/p/13597391.html)
  - systemctl start firewalld
- [添加阿里云安全组规则](https://www.cnblogs.com/cheneyboon/p/11854066.html)

## Redis数据库

> **1.pip安装django-redis扩展包**
>
> **2.django-redis[使用说明文档**](https://django-redis-chs.readthedocs.io/zh_CN/latest/) 
>
> **3.配置`settings.py`**

```python
CACHES = {
    "default": { # 默认的Redis配置项，采用0号Redis库。
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": "redis://127.0.0.1:6379/0",
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
        }
    },
    "session": { # session状态保持的Redis配置项，采用1号Redis库。
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": "redis://127.0.0.1:6379/1",
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
        }
    },
}
SESSION_ENGINE = "django.contrib.sessions.backends.cache"
SESSION_CACHE_ALIAS = "session"
```

> SESSION_ENGINE
>
> - 修改`session存储机制`使用Redis保存。
>
> SESSION_CACHE_ALIAS：
>
> - 使用名为"session"的Redis配置项存储`session数据`。
>
> 配置完成后：运行程序，测试结果。



## 为什么在pycharm里更改html，在浏览器中不变化

因为cache存储在的redis中，需要使用  `flushdb`  清除缓存



## 配置工程日志

收集终端输出的信息，分析客户行为

- logging模块

  ```python
  LOGGING = {
      'version': 1,
      'disable_existing_loggers': False,  # 是否禁用已经存在的日志器
      'formatters': {  # 日志信息显示的格式
          'verbose': {
              'format': '%(levelname)s %(asctime)s %(module)s %(lineno)d %(message)s'
          },
          'simple': {
              'format': '%(levelname)s %(module)s %(lineno)d %(message)s'
          },
      },
      'filters': {  # 对日志进行过滤
          'require_debug_true': {  # django在debug模式下才输出日志
              '()': 'django.utils.log.RequireDebugTrue',
          },
      },
      'handlers': {  # 日志处理方法
          'console': {  # 向终端中输出日志
              'level': 'INFO',
              'filters': ['require_debug_true'],
              'class': 'logging.StreamHandler',
              'formatter': 'simple'
          },
          'file': {  # 向文件中输出日志
              'level': 'INFO',
              'class': 'logging.handlers.RotatingFileHandler',
              'filename': os.path.join(os.path.dirname(BASE_DIR), 'logs/meiduo.log'),  # 日志文件的位置
              'maxBytes': 300 * 1024 * 1024,
              'backupCount': 10,
              'formatter': 'verbose'
          },
      },
      'loggers': {  # 日志器
          'django': {  # 定义了一个名为django的日志器
              'handlers': ['console', 'file'],  # 可以同时向终端与文件中输出日志
              'propagate': True,  # 是否继续传递日志信息
              'level': 'INFO',  # 日志器接收的最低日志级别
          },
      }
  }
  ```


### 日志记录器的使用

```python
import logging

# 创建日志记录器
logger = logging.getLogger('django')
# 输出日志
logger.debug('测试logging模块debug')
logger.info('测试logging模块info')
logger.error('测试logging模块error')
```

- 日志文件不要git，会起冲突；log文件夹要git，否则会报错

  在log文件夹下添加`.gitkeep`文件

## 配置前端静态文件（css、html、js、img等）

```python
STATIC_URL = '/static/'

# 配置静态文件加载路径
STATICFILES_DIRS = [os.path.join(BASE_DIR, 'static')]
```

> 可以通过`http://127.0.0.1:8000/static/images/adv01.jpg`访问静态文件



