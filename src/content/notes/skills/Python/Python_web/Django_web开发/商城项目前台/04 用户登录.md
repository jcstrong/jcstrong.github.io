---
title: "04 用户登录"
category: skills
folderPath: "Python/Python_web/Django_web开发/商城项目前台"
folderTop: "Python"
tags: ["Python", "Web"]
featured: false
source: "Python/Python_web/Django_web开发/商城项目前台/04 用户登录.md"
updated: "2022-05-23"
readingTime: 7
summary: "toc  用户登陆接口设计 https://chenjunxs.osscnhangzhou.aliyuncs.com/img/20210918140738.png 请求方法\tPOST 请求地址\t/login/\t 请求参数：表单\t\t\t\t\t用户..."
---
[toc]

# 用户登陆接口设计

![](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/20210918140738.png)

请求方法	POST
请求地址	/login/	
请求参数：表单					(用户名、密码、是否记住密码)
响应结果：HTML				 (成功：重定向到首页/失败：响应错误信息)



### 用户名登陆登陆逻辑：

- 接收参数

- 校验参数

  `if not all([username, password]):`

  正则

- 认证用户

  `user = authenticate(username=username, password=password)`

- 状态保持 `login(request, user)`

  设置周期	`request.session.set_expiry(0)`

> Django自带的认证authenticate位于django.contrib.auth.backends.ModelBackend
>
> 默认使用用户名认证



### 手机号登陆

- 自定义用户认证

  - 指定自定义的用户认证后端  `settings.py`

  ```
  AUTHENTICATION_BACKEND = ['users.utils.UsernameMobileBackend']
  ```

> 自定义用户认证后端步骤
>
> - 在users应用中新建utils.py文件
> - 新建类，继承自**ModelBackend**
> - 重写认证authenticate()方法
> - 分别使用用户名和手机号查询用户
> - 返回查询到的用户实例



### 首页展示用户名

> 方案一
>
> - 模板中 **request** 变量直接渲染用户名
> - 缺点：不方便做首页静态化

```html
{% if user.is_authenticated %}
    <div class="login_btn fl">
        欢迎您：<em>{{ user.username }}</em>
        <span>|</span>
        <a href="#">退出</a>
    </div>
    {% else %}
    <div class="login_btn fl">
        <a href="login.html">登录</a>
        <span>|</span>
        <a href="register.html">注册</a>
    </div>
{% endif %}
```

> 方案二
>
> - 发送ajax请求获取用户信息
> - 缺点：需要发送网络请求

```html
<div class="login_btn fl">
    {# ajax渲染 #}
</div>
```

> 方案三
>
> - Vue读取cookie渲染用户信息

```html
<div v-if="username" class="login_btn fl">
    欢迎您：<em>[[ username ]]</em>
    <span>|</span>
    <a href="#">退出</a>
</div>
<div v-else class="login_btn fl">
    <a href="login.html">登录</a>
    <span>|</span>
    <a href="register.html">注册</a>
</div>
```

登陆、注册时将用户名写入Cookie

```python
# 响应登录结果
response = redirect(reverse('contents:index'))
# 登录时用户名写入到cookie，有效期15天
response.set_cookie('username', user.username, max_age=3600 * 24 * 15)
return response
```

index.js获取cookie中的用户名

```js
mounted(){
    // 获取cookie中的用户名
    this.username = getCookie('username');
},
```

在模块上加v-cloak，当该模块所有数据获取完毕后再显示，避免出现[[ weijiazaideshuju ]]



### 退出登录

逻辑：重定向到首页、删除cookies中的username

一般实现： `<a href="/logout/" class="quit">退出</a>`

使用jinja2**反向解析**实现：`<a href="{{ url('users:logout') }}">退出</a>`

> jinja2的环境配置在utils文件夹中



### 用户中心

#### 判断用户是否登陆`LoginRequired` Mixin[¶](https://docs.djangoproject.com/zh-hans/3.2/topics/auth/default/#the-loginrequired-mixin)

使用时，视图继承LoginRequiredMixin，并在settings.py中配置未登录时重定向的地址

```
LOGIN_URL = '/login/'
```

到这个地址后，浏览器地址栏显示`http://127.0.0.1:8000/login/?next=/info/`。其中的next是回到重定向前的位置。

```python
# 响应登录结果
next = request.GET.get('next')
if next:
    response = redirect(next)
else:
    response = redirect(reverse('contents:index'))
```

> **next参数的作用**
>
> - 由Django用户认证系统提供，搭配`login_required装饰器`使用。
> - 记录了用户未登录时访问的地址信息，可以帮助我们实现在用户登录成功后直接进入未登录时访问的地址。





# QQ登陆接口设计

请求方法	GET
请求地址	/qq/login/	
请求参数：next（string）					(用于记录QQ登录成功后进入的网址)
响应结果：JSON				 (code	状态码、errmsg	错误信息、login_url	QQ登录扫码页面链接)




### QQ登录开发文档

> [QQ互联开发者申请步骤](https://wiki.connect.qq.com/成为开发者)
>
> [QQ互联应用申请步骤](http://wiki.connect.qq.com/__trashed-2)

[网站对接QQ登录步骤](http://wiki.connect.qq.com/准备工作_oauth2-0)

- 应用申请后得到：

  APP ID: 		101518219
  APP Key: 	 418d84ebdc7241efb79536886ae95224

- 检测网络是否正常：`ping graph.qq.com`

- 防止qq登陆的按钮

- [使用Authorization_Code获取Access_Token](https://wiki.connect.qq.com/使用authorization_code获取access_token)

  GET	https://graph.qq.com/oauth2.0/authorize?response_type=code&client_id=101518219&redirect_uri=http://www.meiduo.site:8000/oauth_callback&state=next



## 业务逻辑

![img](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/20210923172646.png)

### 定义QQ登录模型类

- 为了给项目中模型类补充数据**`创建时间`**和**`更新时间`**两个字段，我们需要定义模型类基类。 在`meiduo_mall.utils/models.py`文件中创建模型类基类。

- 在`oauth/models.py`中定义QQ身份（openid）与用户模型类User的关联关系
- 迁移QQ登录模型类



### QQ登录工具QQLoginTool

封装了QQ登录时对接QQ互联接口的请求操作。可用于快速实现QQ登录。

> **0.QQLoginTool安装**

```bash
pip install QQLoginTool
```

> **1.导入**

```python
from QQLoginTool.QQtool import OAuthQQ
```

> **2.初始化`OAuthQQ对象`**

```python
oauth = OAuthQQ(client_id=settings.QQ_CLIENT_ID, client_secret=settings.QQ_CLIENT_SECRET, redirect_uri=settings.QQ_REDIRECT_URI, state=next)
```

> **3.获取QQ登录扫码页面，扫码后得到`Authorization Code`**

```python
login_url = oauth.get_qq_url()
```

> **4.通过`Authorization Code`获取`Access Token`**

```python
access_token = oauth.get_access_token(code)
```

> **5.通过`Access Token`获取`OpenID`**

```python
openid = oauth.get_open_id(access_token)
```

后端逻辑实现

```python
class QQAuthURLView(View):
    """提供QQ登录页面网址
    https://graph.qq.com/oauth2.0/authorize?response_type=code&client_id=xxx&redirect_uri=xxx&state=xxx
    """
    def get(self, request):
        # next表示从哪个页面进入到的登录页面，将来登录成功后，就自动回到那个页面
        next = request.GET.get('next')

        # 获取QQ登录页面网址
        oauth = OAuthQQ(client_id=settings.QQ_CLIENT_ID,
                        client_secret=settings.QQ_CLIENT_SECRET, 
                        redirect_uri=settings.QQ_REDIRECT_URI, state=next)
        login_url = oauth.get_qq_url()

        return http.JsonResponse({'code': RETCODE.OK, 'errmsg': 'OK', 
                                  'login_url':login_url})
```

> ```python
> QQ_CLIENT_ID = '101518219'
> QQ_CLIENT_SECRET = '418d[REDACTED]'
> QQ_REDIRECT_URI = 'http://www.meiduo.site:8000/oauth_callback'
> ```



### 接收Authorization Code

> 提示：
>
> - 用户在QQ登录成功后，QQ会将用户重定向到我们配置的回调网址。
> - 在QQ重定向到回调网址时，会传给我们一个`Authorization Code`。
> - 我们需要拿到`Authorization Code`并**完成OAuth2.0认证获取openid**。
> - 在本项目中，我们申请QQ登录开发资质时配置的回调网址为：
>   - `http://www.meiduo.site:8000/oauth_callback`
> - QQ互联重定向的完整网址为：
>   - `http://www.meiduo.site:8000/oauth_callback/?code=AE263F12675FA79185B54870D79730A7&state=%2F`

```python
class QQAuthUserView(View):
    """用户扫码登录的回调处理"""

    def get(self, request):
        """Oauth2.0认证"""
        # 接收Authorization Code
        code = request.GET.get('code')
        if not code:
            return http.HttpResponseForbidden('缺少code')
        pass
url(r'^oauth_callback/$', views.QQAuthUserView.as_view()),
```



### OAuth2.0认证获取openid

[关于OAuth2.0](https://blog.csdn.net/yilovexing/article/details/105136883)

```python
# 提取code请求参数
# 使用code向QQ服务器请求access_token
access_token = oauth.get_access_token(code)
# 使用access_token向QQ服务器请求openid
openid = oauth.get_open_id(access_token)
# 使用openid查询该QQ用户是否在美多商城中绑定过用户
# 如果openid已绑定美多商城用户，直接生成JWT token，并返回
# 如果openid没绑定美多商城用户，创建用户并绑定到openid
# 使用code获取access_token
```

openid绑定用户



### itsdangerous的使用

> - `itsdangerous`模块的参考资料链接 http://itsdangerous.readthedocs.io/en/latest/
> - 使用`TimedJSONWebSignatureSerializer`可以**生成带有有效期**的`token`

```python
from itsdangerous import TimedJSONWebSignatureSerializer as Serializer
from django.conf import settings

# serializer = Serializer(秘钥, 有效期秒)
serializer = Serializer(settings.SECRET_KEY, 300)
# serializer.dumps(数据), 返回bytes类型
token = serializer.dumps({'mobile': '18512345678'})
token = token.decode()

# 检验token
# 验证失败，会抛出itsdangerous.BadData异常
serializer = Serializer(settings.SECRET_KEY, 300)
try:
    data = serializer.loads(token)
except BadData:
    return None
```

> **补充：openid签名处理**
>
> - `oauth.utils.py`

```python
def generate_eccess_token(openid):
    """
    签名openid
    :param openid: 用户的openid
    :return: access_token
    """
    serializer = Serializer(settings.SECRET_KEY, expires_in=constants.ACCESS_TOKEN_EXPIRES)
    data = {'openid': openid}
    token = serializer.dumps(data)
    return token.decode()
```

