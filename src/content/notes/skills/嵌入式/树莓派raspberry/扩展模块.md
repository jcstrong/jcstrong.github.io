---
title: "扩展模块"
category: skills
folderPath: "嵌入式/树莓派raspberry"
folderTop: "嵌入式"
tags: ["嵌入式", "树莓派"]
featured: false
source: "嵌入式/树莓派raspberry/扩展模块.md"
updated: 2022-07-06
readingTime: 33
summary: "toc  树莓派GPIO 点击查看源网页https://gimg2.baidu.com/image_search/src=http%3A%2F%2Fosoyoo.com%2Fwpcontent%2Fuploads%2F2017%2F06%2..."
---
[[toc]]

## 树莓派GPIO

![点击查看源网页](https://gimg2.baidu.com/image_search/src=http%3A%2F%2Fosoyoo.com%2Fwp-content%2Fuploads%2F2017%2F06%2FRaspberry-GPIO-Pins_B_plus-1.jpg&refer=http%3A%2F%2Fosoyoo.com&app=2002&size=f9999,10000&q=a80&n=0&g=0n&fmt=jpeg?sec=1619845856&t=f37d52cd26146aee8563d07ee49a5d02)

![858860-20161027174630281-1120046495](https://jcstrong.github.io/img/20210427150915.png)

## 8位 LED 流水灯模块

![Screenshot 2021-03-30 at 9.13.54 PM](https://jcstrong.github.io/img/20210330211404.png)

D0~D7 这 8 个脚位分别控制这 8 个 LED，所以 只要跟树莓派的 GPIO 数字引脚连接起来就好。另外 旁边有三个 RGB 的脚位，需要跟 GND 相连，跟 R 相 连就亮红色，跟 G 相连就亮绿色，跟 B 相连就亮蓝色，三个同时连起来就全都亮



##  4 位 LED 显示模块

*（图片缺失：Screenshot 2021-03-30 at 9.16.12 PM.png）*



这个显示模块有4 个 LED 数码管，采用**共阳极**的接法，由 2 片 74HC595 驱动

引脚：SCLK串行时钟、RCLK接受时钟、DIO数据

4个LED数码管的**位地址**从左到右分别是`0x08、0x04、0x02、0x01`

17个**字模**

`[0xC0,0xF9,0xA4,0xB0,0x99,0x92,0x82,0xF8,0x80,0x90]`

0~9

`[0x8C,0xBF,0xC6,0xA1,0x86,0x8E,0xbf,0x83,0x88]`

P、–、C、d、E、F、–、b、A
驱动方法：字库（16进制）向DIO写入数据，每次向DIO输入一个跳变后要给SCLK一个低电平脉冲，先要写入字模再写入位置（第一个：0x08、第二个：0x04、第三个：0x02、第四个：0x01）

要显示的字模和位地址按字节依次通过DIO口输出到显示模块，依次检查字模和地址字节的每个位，如果是1，则给DIO一个高电平，如果是0，则给DIO一个低电平，
每一位DIO数据输出后，要给串行时钟SCLK一个向上的跳变信号（先低电平、再高电平），
当字模字节和地址字节都通过DIO输出后，要给接收时钟RCLK一个向上的跳变信号（先低电平、再高电平），那么这个位的数据就输出完成，可以输出下一位的数据。

```python
#coding:utf-8
#加载 GPIO 库、time 库 
import RPi.GPIO as GPIO 
import time
GPIO.setmode(GPIO.BCM)
#定义 GPIO 引脚 
SCLK=16
RCLK=20
DIO=21
#设置 GPIO 引脚为输出 
LED_PINS=[16,20,21]
for led_pin in LED_PINS:
	GPIO.setup(led_pin,GPIO.OUT) 
#定义字模
LED_Library=[0xC0,0xF9,0xA4,0xB0,0x99,0x92,0x82,0xF8,0x80,0x90,0x8C,0xBF,0xC6,0xA1,0x86,0x8E,0xbf]
def Show(i_data):
	while i_data>10000: #限制数据大小
		i_data=i_data-10000 
	if(i_data>=1000):
		i_show1=i_data//1000
		i_data-=1000*i_show1
	else: 
		i_show1=0 
	if(i_data>=100):
		i_show2=i_data//100
		i_data-=100*i_show2
	else: 
		i_show2=0 
	if(i_data>=10):
		i_show3=i_data//10
		i_data-=10*i_show3
	else: 
		i_show3=0 
	i_show4=i_data
  #表示4个数码管的位置
	LED_Display(i_show1,0x08) 
	LED_Display(i_show2,0x04) 
	LED_Display(i_show3,0x02) 
	LED_Display(i_show4,0x01)
def LED_Display(i_index,hx_location): 
	LED_OUT(LED_Library[i_index]) #通过上面传入的i_show找到对应字模，LED_OUT
	LED_OUT(hx_location)#输出位置 
#当字模字节和地址 字节都通过DIO输出后，要给接收时钟RCLK一个向上的跳变信号
	GPIO.output(RCLK,GPIO.LOW)#先低电平、再高电平
	GPIO.output(RCLK,GPIO.HIGH)
def LED_OUT(X):
	for i in range(0,8):#依次检查，一个数码管的7段led
		if(X&0x80):#检查字模和地址字节的每个位
			GPIO.output(DIO,GPIO.HIGH) #是1则给 DIO 一个高电平
		else:
			GPIO.output(DIO,GPIO.LOW)#如果是 0，则给 DIO 一个低电平
#每一位DIO数据输出后要给串行时钟SCLK一个向上的跳变信号
		GPIO.output(SCLK,GPIO.LOW)#先低电平、再高电平
		GPIO.output(SCLK,GPIO.HIGH)
		X<<=1#检查下一位
	#主程序 
while (1):
	Show(2020)

```





## AD/DA 模块 PCF8591

![src=http---cbu01.alicdn.com-img-ibank-2017-932-809-3887908239_1075699795.400x400.jpg&refer=http---cbu01.alicdn.com&app=2002&size=f9999,10000&q=a80&n=0&g=0n&fmt=jpeg](https://jcstrong.github.io/img/20210330221221.jpeg)



具有 4 个模拟输入、1 个模拟输出和 1 个串行 I2C 总线接口。

PCF8591 的 3 个地址引脚 A0, A1 和 A2 可用于硬件地址编程，允许在同个 I2C 总线上接入 8 个 PCF8591 器件，而无需额外的硬件。
在 PCF8591 器件上输入输出的地址、控制和数据信号都是通过双线双向 I2C 总 线以串行的方式进行传输。

为了方便使用，板子上装了 3 个模拟信号的产生源

接口：

```
AOUT 芯片 DA 输出接口
AIN0 芯片模拟输入接口 0
AIN1 芯片模拟输入接口 1，接上 P4 短路帽，选择热敏电阻接入电路 （R6）
AIN2 芯片模拟输入接口 2，接上 P5 短路帽，选择光敏电阻接入电路 （R7）
AIN3 芯片模拟输入接口 3，接上 P6 短路帽，选择可变电阻接入电路 （R8）

SCL IIC 时钟接口 接树莓派的 SCL1 口 
SDA IIC 数字接口 接树莓派的 SDA1 口 
GND 模块地 外接地
VCC 电源接口 外接 3.3v
```

```python
address = 0x48 #PCF8591模块地址
A0 = 0x40 #AIN0的地址，内部可变电阻信号
A1 = 0x41 #AIN1的地址，内部光敏输入信号
A2 = 0x42 #AIN2的地址，外部信号，不接时是255
A3 = 0x43 #AIN3的地址，内部热敏信号
bus = smbus.SMBus(1) #在树莓派中，I2C设备位于I2C-1,所以此处的编号为1
global R_value,L_value,H_value #可变电阻信号值、光敏信号值、热敏信号值，全局变量便于数据在多个线程中共享
```



### I$^2$C总线

IC 总线是由 Philips 公司开发的一种简单、双向二线制同步串行总线。它只需要两根线即可在连接于总线上的器件之间传送信息。
主器件用于启动总线传送数据, 并产生时钟以开放传送的器件，此时任何被寻址的器件均被认为是从器件. 在总线上主和从、发和收的关系不是恒定的，而取决于此时数据传送方向。
如果主机要发送数据给从器件，则主机首先寻址从器件，然后主动发送数据至从器件，最后由主机终止数据传送; 
如果主机要接收从器件的数据,首先由主器件寻址从器件. 然后主机接收从器件发送的数据,最后由主机终止接收过程。在这种情况下. 主机负责产生定时时钟和终止数据传送。

```python
def read_thread():#从I2C总线上读取相应数据的线程功能
    global R_value,L_value,H_value
    while True:
        bus.write_byte(address,A0) #向PCF8591模块的A0口写入一个字节，表示启动这个端口
        #从PCF8591模块的A0端口读取一个字节数据，
        R_value = bus.read_byte(address) #读即电阻信号值
        print("R_value:%1.3f "%(R_value)) #显示
        bus.write_byte(address,A1) #读光敏信号值
        L_value = bus.read_byte(address)
        print("L_value:%1.3f "%(L_value))
        bus.write_byte(address,A3) #读热敏信号值
        H_value = bus.read_byte(address)
        print("H_value:%1.3f "%(H_value))
        time.sleep(1) #采样次数一秒一次
        print("===============================")
```

在 Raspbian 图形界面上打开 I$^2$C ;
安装 i2c-tools用来进行硬件监控设备识别和故障诊断
 `sudo apt-get install i2c-tools` 
可以使用 i2c-tool 查询 i2c 设备, 如果树莓派 I2C 接口上有 I2C 设备，则能显示地址。

```python
i2cdetect -y 1
#-y 代表取消用户交互过程，直接执行指令;
#1 代表 I2C 总线编号;
```

在树莓派中，支持 I2C 操作的库是` smbus`，首次使用，需要安装: 
`sudo apt-get install python-smbus`

## 按钮模块

<img src="https://jcstrong.github.io/img/20210331172356.png" alt="Screenshot 2021-03-31 at 5.23.48 PM" style="zoom:33%;" />

不按下，输出高电平；按下，输出低电平。
该模块自带上拉电阻

#### 直接读取引脚的信号

在 GPIO 库中，有一个函数 **`input(channel)`**，可以直接读取某个引脚的信号

```python
#coding:utf-8
import RPi.GPIO as GPIO
import time
KEYs = [27,22,17]
GPIO.setmode(GPIO.BCM)
GPIO.setup(KEYs,GPIO.IN,GPIO.PUD_UP) #设置为上拉模式
while True:
    time.sleep(0.05)
    if GPIO.input(KEYs[1]) == 0:
    	print("KEY PRESS")
    	while GPIO.input(KEYs[1]) == 0:
    		time.sleep(0.01)
```

直接读取引脚信号使用简单方便，但是需要一直占用CPU时间。为了解放出 CPU，可以采用中断的方式来读取按钮的值。

##### ValueError: The channel sent is invalid on a Raspberry Pi

接线问题。检查是Borad还是BCM，检查连线

##### [GPIO 上拉下拉解释说明](https://blog.csdn.net/yinghaijushi/article/details/78991999)

GPIO通常有三种状态：高电平、低电平和高阻态
高阻态：换句话说就是断开状态或浮空态
因此上拉和下拉其中一个强大的理由：就是为了防止输入端悬空，使其有确定的状态。减弱外部电流对芯片的产生的干扰。

上拉就是将不确定的信号通过一个电阻提升为高电平

上下拉的选择：是给IO一个默认的状态 比如控制EN的话，那么高有效的我们就下拉，低有效的话我们就上拉

[RPi.GPIO 模块的输入（Input）功能](https://shumeipai.nxez.com/2016/09/28/rpi-gpio-module-inputs.html)

#### 中断读取

注册中断事件

```python
GPIO.add_event_detect(引脚编号，GPIO.R/F/B，读取信号的延迟时间(ms))
```

第 2 个参数指定是上升沿(GPIO.RISING)还是下降沿(GPIO.FALLING)，或者两者均可(GPIO.BOTH)
第 3 个参数为读取信号的延迟时间，单位是毫秒，用于按键防抖。

绑定中断响应函数

```python
GPIO.add_event_callback(引脚编号，中断响应函数)
```



```python
## -*- coding:utf-8 -*-
import RPi.GPIO as GPIO
import time
KEYS = [17,27,22]
value=10 #全局变量
#定义中断响应函数
def key_callback(channel):
    global value
    if(channel==KEYS[0] and value>0): #接红色键
        value=value-1
    if(channel==KEYS[1]): #接黄色键
        value=0
    if(channel==KEYS[2] and value<9999):#接绿色键
        value=value+1
    print(value)
GPIO.setmode(GPIO.BCM)
GPIO.setup(KEYS,GPIO.IN,GPIO.PUD_UP)
for i in (0,1,2):
 		#给三个引脚注册中断事件，指定下降沿，读取信号延迟事件为200毫秒
    GPIO.add_event_detect(KEYS[i],GPIO.FALLING,bouncetime = 200)
    #给三个引脚绑定中断响应函数
    GPIO.add_event_callback(KEYS[i],callback = key_callback)
while True:
    time.sleep(1)
```



Show(i_data)

​	将i_data表示的数值，显示在4位数码管做上。

​    i_data：要显示在数码管上的数值



LED4_Display(i_index,hx_location)

​	将i_index数字对应的数模，输入到4位数码管的hx_location个位置上显示出来。

​	i_index：在该位要显示的数字

​	hx_location: 表示4位数码管的某一位



LED_OUT(X)

​	将数据X输入到4位数码管模块上

​	X: 一个字节的数据X，可以是字模数据，也可以是地址数据



key_callback(channel)

​	定义中断响应函数，函数中是识别按键，做出相应的加、减、清零等操作

​	channel: 引脚信号



GPIO_init()

​	初始化GPIO设置，如设置GPIO编号方式、设置引脚模式、注册中断事件、绑定中断响应函数



thread1_show()

​	线程1，在数码管上动态显示全局变量value值



thread2_tk()

​	线程2，绘制窗口界面，并循环显示界面内容



thread3_readKey1～3()

​	功能：线程3、4、5，用input(channel)函数读取引脚信号，监听红/绿/黄色按钮信号	

## 超声波模块

![image-20210419182252714](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img9.png)

给 TRIG 引脚至少 10us 的高电平信号触发测距
模块自动发送 8 个 40khz 的方波，自动检测是否有信号返回;
有信号返回，通过 ECHO 输出一个高电平，高电平持续的时间就是超声波从发射到返回的时间。

`测试距离 =(高电平时间*声速(340M/S))/2`

本模块使用方法简单,一个控制口发一个 10US 以上的高电平，就可以在接收口等待高电平输出。一有输出就可以开定时器计时,当此控制口变为低电平时就 可以读定时器的值,此时就为此次测距的时间,方可算出距离.如此不断的周期测,即可以达到你移动测量的值。

操作方法:

初始化：时将 trig 和 echo 端口都置低
				向给 trig 发送至少 10 us 的高电平脉冲
				然后等待，捕捉 echo 端输出上升沿，捕捉到上升沿的同时，打开定时器开始计时
				再次等待捕捉 echo 的下降沿，当捕捉到时， 读出计时器的时间，这就是超声波在空气中运行的时间



## 带编码器的直流电机

### 旋钮电位器

旋钮电位器最大阻值 100KΩ，两端接+5V电压，中间的抽头是分压端子，分压的大小作为给定速度的大小

![image-20210426135806787](https://jcstrong.github.io/img/20210426135806.png)

### 编码器

![image-20210426140357892](https://jcstrong.github.io/img/20210426140357.png)**编码器**是一种将角位移或者角速度转换成一连串电数字有脉冲的旋转式传感器, 我们可以通过编码器测量到底位移或者速度信息。编码器从输出数据类型上分，可以分为增量式编码器和绝对式编码器；从编码器检测原理上来分，还可以分为光学式、磁式、感应式、电容式。常见的是光电编码器〈光学式) 和霍尔编码器 〈磁式) 。

**光电编码器**是一种通过**光电转换**将输出轴上的机械几何位移量转换成脉冲或数字量的传感器。 光电编码器是由光码盘和光电检测装置组成。 光码盘是在一定直径的圆板上等分地开通若干个长方形孔。由于光电码盘与电动机同轴, 电动机旋转时，检测装置检测输出若干脉冲信号，为判断转向，一般输出两组存在一定相位差的方波信号。

**霍尔编码器**是一种通过**磁电转换**将输出轴上的机械几何位移量转换成脉冲或数字量的传感器。 翟尔编码器是由霍尔码盘和霍尔元件组成。霍尔码盘是在一定直径的圆板上等分地布置有不同的磁极。霍尔码盘与电动机同轴，电动机旋转时，霍尔元件检测输出若干脉冲信号，为判断转向，一般输出两组存在一定相位差的方波信号。

可以看到两种原理的编码器目的都是获取 AB 相输出的方波信号，其使用方法也是一样的

这是一款增量式输出的截尔编码器。编码器有 AB 相输出，所以不仅可以测速，还可以辨别转向。根据上图的接线说明可以看到，我们只需给编码器电源5V 供电，在电机转动的时候即可通过 AB 相输出方波信号。编码器自带了上拉电阻，所以无需外部上拉，可以直接连接到单片机 io 读取。

<img src="https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/image-20210426140840849.png" alt="image-20210426140840849" style="zoom:33%;" /><img src="https://jcstrong.github.io/img/20210426135922.png" alt="image-20210426135922009" style="zoom:33%;" />

#### 编码器四倍频技术

这里, 我们是通过软件的方法实现四倍频。首先可以看到上图编码器输出的AB 相波形，正常情况下我们使用M法测速的时候，会通过测量单位时间内A 相输出的脉冲数来得到速度信息。常规的方法，我们只测量A相 (或B相) 的上升沿或者下降沿，也就是上图中对应的数字 1234 中的某一个，这样就只能计数 3次。 而四倍频的方法是测量A相和 B 相编码器的上升沿和下降沿。 这样在同样的
时间内，可以计数12 次 (3个 1234 的循环) 。这就是软件四倍频的原理。

#### 单片机采集编码器数据

因为编码器输出的是标准的方波，所以我们可以使用单片机 (STM32 STMS 51等) 直接读取。在软件中的处理方法是分两种，自带编码器接口的单片机如STM32，可以直接使用硬件计数。而没有编码器接口的单片机如 51 单片机，可以通过外部中断读取，比如把编码器 A 相输出接到单片机的外部中断输入口, 这样就可通过跳变沿触发中断，然后在对应的外部中断服务函数里面, 通过B 相的电

### 电机驱动模块 L298N

L298N 是一种双 H 桥电机驱动芯片，其中每个 H 桥可以提供 2A 的电流，功率 部分的供电电压范围是 2.5-48v，逻辑部分 5v 供电，接受 5v TTL 电平。一般情况下， 功率部分的电压应大于 6V，否则芯片可能不能正常工作。一块 L298N 模块，可以驱动 2 个直流电机或 1 个 4 相步进电机。

![image-20210426141529749](https://jcstrong.github.io/img/20210426141634.png)

板载 5V 使能的作用:
主要对“5V 供电”端子起作用，当采用 12V 供电时，如果接上跳线帽，板子上的的7805 芯片将 12 降到 5 给芯片供电，“5V 供电”端不用外部供电输入，而且还可以输出 5V 电压给其他设备使用;如果拔掉“板载 5V 使能”的跳线帽，则板子需要外部电源提供给 “5V 供电”。通常是为了避免稳压芯片损坏，当输入驱动电压大于 12V 时，拔掉跳线帽， 由外部电源输入 5V 电压为驱动板供电。

### PID 算法

在过程控制中，按偏差的比例(P)、积分(I)和微分(D)进行控制的 PID 控制器(亦称 PID 调节器)是应用最为广泛的一种自动控制器。

#### PID 公式:

![image-20210426142007508](https://jcstrong.github.io/img/20210426142007.png)

#### pid代码

```c++
/*working variables*/
unsigned long lastTime;
double Input, Output, Setpoint;
double errSum, lastErr;
double kp, ki, kd;

void Compute()
{
  /*How long since we last calculated*/
  unsigned long now = millis();
  double timeChange = (double)(now - lastTime);
  
  /*Compute all the working error variables*/
  double error = Setpoint - Input;
  errSum += (error * timeChange);
  double dErr = (error - lastErr) / timeChange;
  
  /*Compute PID Output*/
  Output = kp * error + ki * errSum + kd * dErr;
  
  /*Remember some variables for next time*/
  lastErr = error;
  lastTime = now;
}

void SetTunings(double Kp, double Ki, double Kd)
{
  kp = KP;
  ki = Ki;
  kd = Kd;
}
```

#### PID 参数的整定方法

##### 临界振荡过程

控制系统在外界干扰作用后，不能恢复到稳定的平衡状态，而出现一种既不衰减，也不发散的等幅振荡过程，这样的过渡过程就称为临界振荡过程，如图 1 所示。
我们在临界比例度法整定中，首先需要得到的就是临界参数，即在临界状态下，被控量 y 来回振荡一次所用时间，称为临界周期 Tk;被调参数处于临界状态时的比例度，称为临界比例度δk。

![image-20210426142304107](https://jcstrong.github.io/img/20210426142304.png)

用临界比例度法整定 PID 参数时，要在纯比例作用下，在控制系统中由大到小的改变得到我们所需要的临界比例度δ~k~ 和临界周期 T~k~ 的数值。然后再根据经验公式，计算出调节器各参时间数的具体数值。

##### 临界比例度法整定口诀

临界整定应用多，纯 P 运转减参数;
等幅振荡出现时，δk 值 Tk 值为临界;
按照公式乘系数，P、I、D 序不能错;
静观运行勤调整，细心寻求最佳值。

##### 临界比例度法整定步骤 

先把积分时间放至最大，微分时间放至零，比例度放至较大的适当值。
“纯 P 转减参数”，就是使控制系统按纯比例作用的方式投入运行。然后慢慢地减少比例度，在外界干扰的作用下，细心观察调节器的输出信号和被调参数的变化情况;
如果控制过程的曲线波动是衰减的，则把比例度继续调小，
如果控制过程的曲线波动是发散的，则应把比例度调大些，直到曲线波动呈等幅振荡为止，
以此得到临界振荡过程，从而得到临界比例度δk 和临界周期 Tk 值。即口诀说的“等幅振荡出现时，δk 值 Tk 值为临界”。

“按照公式乘系数”即根据得到的δk 和 Tk 值按表 1 临界比例度法参数计算公式表， 来计算调节器的各参数值。![image-20210426142934392](https://jcstrong.github.io/img/20210426142934.png)



求得具体的数值后，将比例度调在比计算数值大一些的刻度上，然后把积分系数放至计算，然后从小到大地调整积分系数，最后把微分系数放至计算值上，从小到大地调整微分时间。这样的调整次序就是口雇中的**“PID 序不能错”**。

4最后把比例度减小到计算值上，通过观察曲线，也就是**“静观运行勤调整， 细心寻求最佳值”**。即适当地进行各参数的微调，以达到满意的控制效果。 经典的经验试凑口诀:

参数整定找最佳， 从小到大顺序查。
先是比例后积分， 最后再把微分加。
曲线振荡很频繁， 比例度盘要放大。
曲线漂浮绕大弯,    比例度盘往小扳。
曲线偶离回复慢， 积分时间往下降。
曲线波动周期长， 积分时间再加长。
曲线振沪频率快， 先把微分降下来。
动差大来波动慢,    微分时间应加长。
理想曲线两个波， 前高后低四比一。
一看二调多分析， 调节质量不会低。

### 回调函数编程方法

由于树莓派不像 arduino 或者单片机那样可以设定定时器或者中断模式，我们在测量电机转速时需要用到回调函数，在每一个编码器输出的 PWM 的上升沿(或者下降沿)触发事件回调函数，在回调函数中对转速进行计算。其示例代码如下:

```python
def my_callback(channelA):
  global counterA,start0,startl,end0,endl #DIS Pwm
  #脉冲数 counterA，时间的起始和结束需要定义为全局变量或类变量以免下次调用时丢失
  if GPIO.event_detected(8):
    counterA=counterA+1 #每检测到一次上升沿计数+1
    end0 = datetime.datetime.now()
    if (endO-start0).total seconds()>=0.01: #每 10ms 计算一次转数
      Pwm_Encoder = counterA/float((end0-start0).total_seconds())
      end1 = datetime.datetime.now()
      if(end1-start1).total_seconds() >= 0.1:
      global Encoder_Pwm
      Encoder_Pwm = Pwm_Encoder
      start1 = end1
    #电机减速比为 30: 13 线,因此车轮每转一圈，脉冲数为390
    counterA = 0
    start0=end0
    value = bus.read_byte(address)#AD 读出的电位器值
    global Pwm_target
    #电机满速设置为车轮每秒转6 圈 6X390=2340
    Pwm_target = value/255.0*2340.0
    Pwm_OUT=Incremental_PI(Pwm_Encoder,Pwm_target)/2340.0*100.0 if Pwm_OUT < 0:
    if Pwm_OUT<0:
      Pwm_OUT = 0 
    if Pwm_OUT > 100:
      Pwm_OUT = 100 
    pwm.start(Pwm_OUT)
```

### 多线程编程方法 

由于实验中需要在控制电机转速的同时刷新数码管动态显示，而测量电机转速的周期一般在 10ms 以内，同时为了保证数码管各位的亮度一致，刷新每一位时需要一定的延时时间， 这样就会使两个任务相互干扰，这里我们采用了多线程的解决办法，使控制电机转速的程序和显示刷新的程序运行在不同的线程当中，他们之间的参数传递使用消息队列或者共享内存，保证两个任务互不干扰。示例代码如下:

```python
t1 = Thread(target=LED_Encoder,args=(1,)) 
t2 = Thread(target=LED_Target,args=(1,)) 
t1.start()
t2.start()
```

创建执行不同功能子函数的线程，然后启动他们

![image-20210426135619375](https://jcstrong.github.io/img/20210426135619.png)

## 温湿度模块BME/P280

![image-20210426150421040](https://jcstrong.github.io/img/20210426150421.png)



![Screenshot 2021-04-26 at 3.04.36 PM](https://jcstrong.github.io/img/20210426150534.png)



[读取温度值](https://www.cnblogs.com/leptonation/p/5210331.html)

修整参数在生产期间已编程到设备的非易失性存储器（NVM）中，客户无法更改。每个补偿字都是一个以两位补码存储的16位有符号或无符号整数值。由于存储器被组织成8位字，因此必须始终将两个字组合起来以表示补偿字。这8位寄存器的名称为calib00 ... calib41，存储在存储器地址0x88 ... 0xA1和0xE1 ... 0xE7中。对应的补偿字被命名为dig_T＃（用于温度补偿相关值），dig_P＃（用于压力相关值）和dig_H＃（用于湿度相关值）。映射见表16。

![Screenshot 2021-04-28 at 7.18.22 PM](https://jcstrong.github.io/img/20210428203451.png)![Screenshot 2021-04-28 at 7.18.42 PM](https://jcstrong.github.io/img/20210428203502.png)















模块I2C模式下通过SDO引脚来确定器件地址，
SDO接GND，低电平从机地址为1110110 (0x76) ,本模块默认0x76,; 
接高电平从机地址1110111 (0x77)

```python
#I2C 设备地址由 SDO 引脚电平决定 address = 0x76
#温度寄存器地址
Temp_MSB = 0xfa
Temp_LSB = 0xfb
Temp_XLSB = 0xfc 
#湿度寄存器地址
Hum_MSB = 0xfd
Hum_LSB = 0xfe
#气压寄存器地址
Press_MSB = 0xf7
Press_LSB = 0xf8
Press_XLSB = 0xf9
bus = smbus.SMBus(1) #初始化设备寄存器 bus.write_byte_data(address,0xe0,0xb6) #配置传感器工作模式 bus.write_byte(address,0xd0) bus.read_byte(address) bus.write_byte_data(address,0xf4,0xff) bus.write_byte_data(address,0xf5,0x00)
#读取温度
bus.write_byte(address,Temp_MSB)
T_value1 = bus.read_byte(address)
bus.write_byte(address,Temp_LSB)
T_value2 = bus.read_byte(address)
bus.write_byte(address,Temp_XLSB)
T_value3 = bus.read_byte(address) Temp=(T_value1<<16|T_value2<<8|T_value3)>>4 #温度转换公式,各项转换公式参考 datasheet
T_var1 = ((((Temp>>3)-(dig_T1<<1)))*dig_T2)>>11
T_var2 = (((((Temp>>4)-dig_T1)*((Temp>>4)-dig_T1)>>12))*dig_T3)>>14 t_fine = T_var1+T_var2
T = (t_fine*5+128)>>8
#读取湿度
bus.write_byte(address,Hum_MSB)
H_value1 = bus.read_byte(address)
bus.write_byte(address,Hum_LSB)
H_value2 = bus.read_byte(address)
Hum=(H_value1<<8|H_value2)
#湿度转换公式
H_var = t_fine - 76800
H_var=((((Hum<<14)-(dig_H4<<20)-
(dig_H5*H_var))+16384>>15)*(((((((H_var*dig_H6)>>10)*(((H_var*dig_H3)>>11)+32768))>> 10)+2097152)*dig_H2+8192)>>14))
H_var = (H_var-(((((H_var>>15)*(H_var>>15))>>7)*dig_H1)>>4)) if H_var<0:
H_var = 0
if H_var>419430400:
H_var = 419430400 H = H_var>>12
H = float(H)/1024.0*100
```

## 蓝牙HC-05模块

![image-20210426151458448](https://jcstrong.github.io/img/20210426151458.png)

### USB 转串口模块

USB 转串口模块用于连接树莓派和 HC-05 蓝牙模块，树莓派插上 USB 转串口模块后，

在/dev 目录下将出现 ttyUSB0 设备，然后将 USB 转串口模块与 HC-05 模块通过杜邦线连接 起来，方法是，USB 转串口模块的 TXD 接 HC-05 模块的 RXD，USB 转串口模块的 RXD 接 HC-05 模块的 TXD。

开启树莓派蓝牙

```perl
sudo apt-get update
sudo apt-get upgrade -y
sudo apt-get dist-upgrade -y
sudo apt-get install pi-bluetooth bluez bluez-firmware blueman
#添加pi用户到蓝牙组
sudo usermod -G bluetooth -a pi
sudo reboot
```

右键点击待连接HC-05 模块，选择 serial port，连接成功后，/dev 目录下会出现 rfcomm0 设备

```python
import RPi.GPIO as GPIO
ser = serial.Serial("/dev/ttyUSB0",9600) 
count = ser.inWaiting()
if count != 0:
  recv = ser.read(count)
  print('I am recv:',recv)
```

## 摄像头模块

激活摄像头模块。终端输入命令:`sudo raspi-config`

CSI 摄像头传感器节点设置。

首先查看是否启动 video0，终端输入命令`ls -l /dev/video0`
如果没有发现 video0，则修改` /etc/modules `文件，终端输入命令`sudo vi /etc/modules `文本末添加:`bcm2835-v4l2` 再次查看`/dev/video0`，终端输入命令:`ls -l /dev/video0`

### OpenCV 

为 Open Source Computer Vision 的缩写，是一个基于 BSD 许可(开源)发行

安装` opencv-python`
sudo pip3 install opencv_python

安装相关依赖

```
sudo apt-get install libhdf5-dev
sudo apt-get install libhdf5-serial-dev 
sudo apt-get install libatlas-base-dev 
sudo apt-get install libjasper-dev 
sudo apt-get install libqtgui4
sudo apt-get install libqt4-test
```

大多数常用的 OpenCV 函数都在 cv2 模块内，可能也会遇到其他基于 cv 或 cv2.cv 模块的 OpenCV 帮助，这些都是传统版本。Python 模块被称为 cv2 并不是表示该模块 是针对 OpenCV 2.x.x 版本的，而是该模块引入了一个更好的 API 接口，他们采用了面向对象的编程方式，这与以前的 cv 模块有所不同，以前的 cv 模块更多的采用过程化的 编程方式。

读入图像- imread()函数

使用函数 cv2.imread() 可以读入图像。第一个参数表示图像文件的路径，第二个参数是 读取图像文件的方式:
cv2.IMREAD_COLOR:读入一副彩色图像。图像的透明度会被忽略， 这是默认参数。 cv2.IMREAD_GRAYSCALE:以灰度模式读入图像。

摄像头的数量和顺序由系统决定。但 OpenCV 没有提供任何查询摄像头数量和属性的 方法。如果使用无效索引构造了 Video Capture 类，就不会得到帧， Video Capture 的 read() 函数会返回( false, None)。

有时 camera 可能不能成功的初始化摄像头设备。这种情况下上面的代码会报错。你可 以使用 camera.isOpened()，来检查是否成功初始化了。如果返回值是 True，那就没有问题。 否则就要使用函数 camera.open()。

你可以使用函数 camera.get(propId) 来获得视频的一些参数信息。这里 propId 可以是 0 到 18 之间的任何整数。

其中的一些值可以使用 camera.set(propId,value) 来修改，value 就是你想要设置成的新

值。例如，我可以使用 camera.get(3) 和 camera.get(4) 来查看每一帧的宽和高。默认情况 下得到的值是 640X480。但是我可以使用 ret= camera.set(3,320)和 ret= camera.set(4,240) 来 把宽和高改成 320X240。

### Mac 安装OpenCV

流程如下:
1,下载brew

```
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"  
```

2,下载opencv

```
brew install opencv
```

3,打开site-pakage

```
cd /Users/!!!!!这里要填写自己的用户名!!!!!/anaconda3/lib/!!!!!这里要填写自己的python+版本号!!!!!/site-packages
```

4,导入opencv

```
ln -s /usr/local/Cellar/opencv/3.3.1_1/lib/python3.6/site-packages/cv2.cpython-36m-darwin.so
```

5,完成,测试
[![没有报错说明已成功导入](https://img.it610.com/image/info8/a91d3a138e0d42e3ab4788c21b9f3a23.png)](https://img.it610.com/image/info8/a91d3a138e0d42e3ab4788c21b9f3a23.png)
没有报错说明已成功导入

这个是给vscode用的,按照这个过程操作,你可以在**vscode中使用opencv**

1,打开vscode

2,在**vscode的termianl中**输入代码:

```
python -m pip install opencv-python
```

3,效果如下
[![如何在mac上vscode里给python装上opencv_第1张图片](https://img.it610.com/image/info8/b27c6d5ddef14bb58a530592ea5886ab.jpg)](https://img.it610.com/image/info8/b27c6d5ddef14bb58a530592ea5886ab.jpg)

4,完成
[![拒绝报错](https://img.it610.com/image/info8/2573a038d6b6484999f4d1e3c4ba5bf5.jpg)](https://img.it610.com/image/info8/2573a038d6b6484999f4d1e3c4ba5bf5.jpg)
没有报错说明已成功导入

5,如果需要使用opencv中的复杂模块,需要单独在下载一个opencv-lib

```
python -m pip install opencv-contrib-python
```

https://www.it610.com/article/1304943225659756544.htm

### 对象检测

(1 Haar级联的概念

当谈到目标分类和位置跟踪时, 希望精确定位什么? 什么才是目标的可识别部分?

摄影作品《〈甚至是来自网络摄像头的图像) 可能包含很多令人愉悦的细节。但是，由于
灯光、视角、视距、摄像头抖动以及数字噪声的变化, 图像细节变得不稳定。人们在分类时
不会受这些物理细节方面差异的影响。

因此, 提取出图像的细节对产生稳定分类结果和跟踪结果很有用。这些提取的结果被称
为特征,专业的表述为:从图像数据中提取特征。虽然任意像素都可能影响多个特征, 但特征
应该比像素数少得多。两个图像的相似程度可以通过它们对应特征的欧氏距离来度量。

例如, 距离可能以空间坐标或颜色坐标来定义。类 Har 特征是一种用于实现实时人脸跟
踪的特征。文献《 Robust real- Time Face Detection, Paul viola and Michael Jones, Kluwer
AcademicPublishers2001)(http://www.vision.caltech.edu/html-files/ee148-2005-spring/pprs/violao4 ijcv. pdfb) 首次采用这种特征来进行人脸检测 。每个类 Haar 特征都描述了
相邻图像区域的对比模式。例如, 边、顶点和细线都能生成具有判别性的特征。

对给定的图像，特征可能会因区域大小而有所不同, 区域大小也可被称为窗口大小
(window size) 。即使窗口大小不一样, 仅在斥度上不同的两幅图像也应该有相似的特征。
此, 能为不同大小的窗口生成特征非常有用。这些特征集合称为级联。Haar 级联具有尺度不
变性, 换句话说，它在尺度变化上具有和鲁棒性。 OpenCyV 提供了尺度不变 Haar 级联的分类
器和跟踪器，并可将其保存成指定的文件格式。

OpenCy 的 Har 级联不具有旋转不变性。例如, ，Haar 级联不认为倒置的人脸图像和直
工的人脸图像一样，而侧面的人脸图像与正面的人脸图像也不一样。更可通过多种图像变换
oe 口大小来提高 Haar 级联的旋转鲁棒性, 但这样会变得很复杂, 而且会耗费更多计算

获取 Haar 级联数据
 在 OpenCv3 源代码的副本中会有一个文件夹 data。该文件夹包含了所有 OpenCV 的

人脸检测的 XML 文件，这些文件可用于检测静止图像、视频和摄像头所得到图像中的人脸。

## RFID 读卡器MFRC522

我们现在可以检查以确保它实际上已启用。最简单的方法是运行lsmod | grep spi命令，查看是否列出 **spi_bcm2835**。

`lsmod | grep spi`

ISO-14443A协议:( 国际标准化组织:InternationalOrganizationforStandardization)RFID 协议的一种;

PICC:临近卡(分为 A 型和 B 型)、PCD:临近耦合设备--指 MFRC522 模块、 ATQ:对请求的应答、ATQA 对 A 型卡请求的应答、ATQB 对 B 型卡请求的应答

REQA:对 A 型卡的请求、REQB:对 B 型卡的请求、WUPA:A 型卡的唤醒命令(更多 缩写参考百度:ISO14443)

ASK:选择确认
 2MIFARE: 恩智浦半导体(NXPSemiconductors)拥有的商标之一;
 3MIFARE 卡(M1 卡):兼具读写功能，主要是非接触式，有快速防冲突机制 – 处理多卡同 时进入感应区的机制，其特殊的存储结构适合一卡多用，目前主要芯片 philip mifare1 S50、 S70 等;
 4官方网站:http://www.cn.nxp.com/ 网站中可以下载 MFRC522 芯片手册和各种智能卡的使 用手册。
 5MFRC522 特性

非接触式读写卡芯片、最大通信速率 13.56MHz、 支持接口:UART、SPI(Speed_max=10Mbit/s)、IIC(快速:400Kbit/s,高速:3400Kbit/s) 通信协议:ISO-14443A 或者叫 MIFARE
 收发缓冲:64 字节
 支持中断模式、可编程定时器、CRC 协处理器

2、M1 卡结构
 共 16 个扇区，每个扇区 4 块(块 0~3)，共 64 块，按块号编址为 0~63。
 第 0 扇区的块 0(即绝对地址 0 块)用于存放厂商代码，已经固化，不可更改。其他各扇区: 块 0、块 1、块 2 为数据块，用于存贮数据;块 3 为控制块，存放密码 A、存取控制、密码 B。
 对块 3 中蕴藏的原理的理解是操作 RFID 模块的关键所在。每个扇区的密码和存取控制都是 独立的，可以根据实际需要设定各自的密码及存取控制。注意加密是针对一个扇区来进行的， 千万不要理解成对每一个数据块进行加密!
 在 MF1 S50 卡中，1024*8 bit EEPROM 存储器分为 16 个扇区，每区 4 块，每块 16 字节。 在擦除后的状态下，EEPROM 的单元读为逻辑“0”，写后的状态读为“1”。EEPROM 结构 如下所示:

![image-20210426154748850](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/image-20210426154748850.png)

![image-20210426154802022](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/image-20210426154802022.png)

![image-20210426154820216](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/image-20210426154820216.png)

![image-20210426154829750](https://chenjun-xs.oss-cn-hangzhou.aliyuncs.com/img/image-20210426154829750.png)

![image-20210426154849686](https://jcstrong.github.io/img/20210426154849.png)

称为获得所有的请求。获得请求之后 M1 卡发送回应 ATQA 码。ATQA 是卡的类型码:S50 的类型码是 0x04，S70 的类型码是 0x02。
 (2)M1 卡获得请求后再次获得防冲突命令执行防冲突机制。情景分析:有这么一种情况，一 个时刻多张卡同时出现在 MFRC522 感应区，这时候 RC522 必须要专一，同时和几张卡发 生关系早晚都是要出事的，那么 RC522 必须要认定其中一张卡。当然事情还要讲究两厢情 愿是吧，522 问人家了，这就要求 M1 卡能够主动告知 RC522“我就是你的唯一的标识”。于 是，事情就变成了这样:RC522 获得最先主动告诉它的 M1 卡的 UID，即卡的 SN(4Bytes)+ 校验(1Byte) ，作为下一步选择通信卡的依据，这个 SN 需要主机从 MFRC522 中读出来暂 时保存。

(3)选卡:在主机控制下 MFRC522 需要把选卡的命令和上一步获取的卡 SN 依次发送给 M1 卡，卡收到信息之后很老实，是选自己的信息就主动返回 SAK 信息，1Byte，实际上就是卡 片的容量。
 (4)验证密码:这是极其关键的一步了，里面涉及比较多比较复杂的协议。在 PCD 选中卡之 后，接着主机控制他指出将要访问的存储器位置，然后连同验证密码命令、验证秘钥 A|B 一 起发送给卡，三次密码确认(硬件)成功之后，MFRC522 的 Status2Reg 的 Bit_4 置位，之后的 数据传输都是经过加密的。(对一个扇区一次验证密码成功之后根据控制字的设置可以对该 扇区的数据块进行相应读写操作直到执行 Halt 终止或验证其他扇区的密码，在选择另一扇 区时，需要先进行这一扇区密码校验。细心观察上面那幅流程图的箭头也可以发现这个要求) 此外，有一点必须要说明的就是:默认控制下 FF07806 时 A 秘钥:不可读出，具有所有权 限;B 秘钥:可读出，没有任何权限。控制字和秘钥之间的联系，推荐使用这个软件: (5)存储器操作:最终目的。数据块写、数据块读:通过向 M1 卡发送 WRITE、READ 命令， 分别是 0xA0、0x30;数据块增值、数据块减值:通过发 M1 卡的 INCREMENT、DECREMENT 指令，分别是 0xC1、0xC0;(写数据块时命令连同数据值一块发送。) (6)终止卡操作:HALT 指令进入终止休眠模式。

(7)数据完整性措施:PCD 和 PICC 之间的数据传输
 每个块 16 位 CRC 校验、每个字节都有奇偶校验位、位计数检查、位编码区别“1”“0”“无信 息”、信道监控。
 CRC16 校验: MFRC522 内部有 CRC16 协处理器，主机通过软件设置 CRC 初始值，然后 把要发送的数据发送给 MFRC522，激活 CRC16 协处理器后，等待一小会读取 CRCResultReg 寄存器值即 CRC16 校验值，然后把这个 16 位的校验值附加到数据值后再通过 MFRC522 发 送给 M1 卡片。至于其他校验是硬件自动完成的，我们只需要自己去读取 MFRC522 的 Status1Reg 寄存器看检验完成情况，读 ErrorReg 寄存器就可知道成功与否。 5、在树莓派上操作 MFRC522:MFRC522 的全部工作流程过于复杂，官方也未提供标准库 支持，故在本实验附录中提供 MFRC522 相关操作的第三方库源码，请同学们根据工作流程 自行理解和使用

## 屏幕

### 如何安装LCD驱动(CN)

### **树莓派LCD驱动安装说明（方法1: 在线安装）**

本文适用于全系列树莓派液晶屏，安装过程树莓派主板需连接互联网操作 

### 步骤1, 安装Raspbian官方镜像 

1）从官方下载最新Raspbian镜像 https://www.raspberrypi.org/downloads/ 
2）使用SDFormatter格式化Micro SD卡 
3）使用Win32DiskImager把官方镜像烧录到Micro SD卡 

### 步骤2, 获取LCD驱动 

登陆树莓派系统到用户命令行(初始用户名: pi 密码: raspberry), 从**GitHub**获取最新驱动(树莓派需连接互联网)

```
sudo rm -rf LCD-show
git clone https://github.com/goodtft/LCD-show.git
chmod -R 755 LCD-show
cd LCD-show
```

### 步骤3, 安装LCD驱动 

- 如使用2.4寸树莓派LCD (MPI2401) 则对应执行:

```
sudo ./LCD24-show   
```

- 如使用2.4寸树莓派LCD For RPi 3A+ (MPI2411) 则对应执行:

```
sudo ./LCD24-3A+-show   
```

- 如使用2.8寸树莓派LCD (MPI2801) 则对应执行:

```
sudo ./LCD28-show   
```

- 如使用3.2寸树莓派LCD (MPI3201) 则对应执行:

```
sudo ./LCD32-show   
```

- 如使用MHS-3.2寸树莓派LCD (MHS3232) 则对应执行:

```
sudo ./MHS32-show   
```

- 如使用3.5寸树莓派LCD (MPI3501) 则对应执行:

```
sudo ./LCD35-show   
```

- 如使用3.5" HDMI Display-B(MPI3508)则对应执行:

```
sudo ./MPI3508-show
```

- 如使用MHS-3.5寸树莓派LCD (MHS3528) 则对应执行:

```
sudo ./MHS35-show   
```

- 如使用4.0寸树莓派LCD (MPI4008) 则对应执行:

```
sudo ./MPI4008-show   
```

- 如使用MHS-4.0寸树莓派LCD-B (MHS4001) 则对应执行:

```
sudo ./MHS40-show   
```

- 如使用5寸树莓派电阻触摸屏 (MPI5008) 则对应执行:

```
sudo ./LCD5-show    
```

- 如使用5寸树莓派电容触摸屏 (MPI5001) 则对应执行:

```
sudo ./MPI5001-show    
```

- 如使用7inch HDMI Display-B-800X480(MPI7001) 则对应执行:

```
sudo ./LCD7B-show   
```

- 如使用7inch HDMI Display-C-1024X600(MPI7002) 则对应执行:

```
sudo ./LCD7C-show   
```

- （如需切换回传统HDMI显示器）则对应执行:

```
sudo ./LCD-hdmi     
```

- 执行完上述命令后稍等片刻，即可使用对应的树莓派LCD。

### **树莓派LCD驱动安装说明（方法2: 离线安装）**

本文适用于全系列树莓派液晶屏，如客户已有LCD驱动安装光盘，可离线操作 

### 步骤1, 安装Raspbian官方镜像 

1）从官方下载最新Raspbian镜像 https://www.raspberrypi.org/downloads/ 
2）使用SDFormatter格式化Micro SD卡 
3）使用Win32DiskImager把官方镜像烧录到Micro SD卡 

### 步骤2, 获取LCD驱动 

从配套光盘内提取或咨询卖家索取，将[LCD-show.tar.gz](http://www.lcdwiki.com/res/RaspDriver/LCD-show.tar.gz)驱动复制到树莓派系统根目录下 （推荐**步骤1**烧录完成后将驱动直接拷贝到Micro SD卡，或使用SFTP等办法远程拷贝）执行以下操作命令解压驱动:

```
cd /boot
sudo tar zxvf LCD-show.tar.gz
cd LCD-show/
```

### 步骤3, 安装LCD驱动 

安装指令与【方法1 步骤3一样】:[返回步骤3](http://www.lcdwiki.com/zh/如何安装LCD驱动(CN)#.E6.AD.A5.E9.AA.A43.2C_.E5.AE.89.E8.A3.85LCD.E9.A9.B1.E5.8A.A8)

