---
title: "论文"
category: projects
folderPath: "毕业"
folderTop: "毕业"
tags: ["知识图谱", "CRF", "学术"]
featured: true
source: "毕业/论文.md"
updated: 2023-04-25
readingTime: 7
summary: "https://www.elastic.co/cn/blog/textsimilaritysearchwithvectorsinelasticsearch 论文 马志鑫的ppt 第四章  文本向量表示  BERT+LSTM+CRF实体识别 ..."
---
https://www.elastic.co/cn/blog/text-similarity-search-with-vectors-in-elasticsearch





**论文**



马志鑫的ppt





第四章

- 文本向量表示
- BERT+LSTM+CRF实体识别
- 同义词：文本相似度实体对齐
- 检索的向量匹配
- 





- 词典构建（实体识别）

- - 三个文本字段的数据库用哪个

  - ik分词器的字典

  - - 改写源码进行更新

- 同义词词典构建（文本相似性，实体对齐）

- - https://zhuanlan.zhihu.com/p/55981383

- 数据预处理（预处理）（创建ES索引的过程）

- - 配件id？

- 数据抽取（Logstash数据抽取）

- - 索赔单在哪？
  - 马志鑫 验证

- 故障知识检索服务（句子相似性、倒排索引）

- 输入索赔单提示服务

- - 实时识别
  - 纠错

- 实时更新的数据

csdn账号密码



账号：15952902695

密码：qca888888888







导出为csv

*（图片缺失：10c55c45-f83f-409f-89d0-7e0cef278f26）*









```sql
--select F_CarSeries, COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim where F_ClaimName is NULL Group by F_CarSeries;

--select F_CarSeries, COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim where F_ClaimName like N'唐%' Group by F_CarSeries;

-- SELECT x.* FROM cj_fault_serve.dbo.F_S_Claim x WHERE F_ClaimName like N'凯%'

-- SELECT F_CarSeries, COUNT(*) FROM cj_fault_serve.dbo.F_S_Claim x WHERE F_ClaimName like N'凯%' Group by F_CarSeries;

-- select F_ClaimName, COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim Group by F_ClaimName;线·

--SELECT COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim where year(F_InputTime)='2020' AND F_ClaimName like N'凯%' Group by MONTH(F_InputTime);
--select COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim where year(F_InputTime)='2021' AND F_ClaimName not like N'凯%' and F_ClaimName not like N'唐%' Group by MONTH(F_InputTime);

SELECT COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim where year(F_InputTime)='2021' AND F_ClaimName like N'唐%' Group by month(F_InputTime);



select 
month(F_InputTime) as month, day(F_InputTime) as day,
count(F_InputTime) as count
from  cj_fault_serve.dbo.F_S_Claim
where  year(F_InputTime)='2021' and F_ClaimName not like N'凯%' and F_ClaimName not like N'唐%'
group by month(F_InputTime), day(F_InputTime)
ORDER  by month,day




--获得维修的配件故障
SELECT 
fsp.P_PartName,fsp.F_FaultName,fsc.B_CorpName,fsp.P_PartCode,fsp.P_PartType,fsp.P_PartSpec,
fsc.F_CarModels  ,fsc.F_FaultDesc ,fsc.F_FaultReason ,fsc.F_SolveFault 
from 
F_S_ClaimParts fsp join F_S_Claim fsc 
on
fsp.F_Mid_ClaimId = fsc.F_PKId 






SELECT COUNT(*)  FROM cj_fault_serve.dbo.F_S_Claim

select into  partdict DISTINCT(P_PartName) from F_S_ClaimParts fscp 



INSERT INTO partdict (
P_PartName
)
SELECT DISTINCT
P_PartName
FROM F_S_ClaimParts



SELECT x.* into cj_fault_serve.dbo.S_Claim FROM cj_fault_serve.dbo.F_S_Claim x
WHERE B_CorpName like N'成都金牛%'


INSERT INTO cj_fault_serve.dbo.S_Claim(P_PartName)
SELECT P_PartName
FROM cj_fault_serve.dbo.F_S_ClaimParts y 
WHERE F_Mid_ClaimId = cj_fault_serve.dbo.S_Claim.F_PKId 



INSERT INTO cj_fault_serve.dbo.S_Claim(P_PartName)
SELECT x.F_PKId
FROM cj_fault_serve.dbo.F_S_ClaimParts y 
join cj_fault_serve.dbo.S_Claim x 
on x.F_PKId = y.F_Mid_ClaimId
where cj_fault_serve.dbo.S_Claim.F_PKId  = cj_fault_serve.dbo.F_S_ClaimParts.F_Mid_ClaimId 

DELETE FROM cj_fault_serve.dbo.S_Claim
where F_PKId NOT in
(SELECT x.F_PKId
FROM cj_fault_serve.dbo.S_Claim x 
join cj_fault_serve.dbo.F_S_ClaimParts y 
on x.F_PKId = y.F_Mid_ClaimId)







SELECT y.P_PartName,y.F_Mid_ClaimId
FROM cj_fault_serve.dbo.S_Claim x join cj_fault_serve.dbo.F_S_ClaimParts y 
on x.F_PKId = y.F_Mid_ClaimId


SELECT P_PartName INTO cj_fault_serve.dbo.S_Claim FROM cj_fault_serve.dbo.F_S_ClaimParts WHERE 1=0;

SELECT y.P_PartName,y.F_Mid_ClaimId INTO B
FROM cj_fault_serve.dbo.S_Claim x join cj_fault_serve.dbo.F_S_ClaimParts y 
on x.F_PKId = y.F_Mid_ClaimId


UPDATE S_Claim
SET P_PartName = B.P_PartName
FROM S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId


SELECT B.P_PartName,x.F_ClaimCode 
FROM S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId


-- 设置1-n的ID值
WITH cte AS (
    SELECT ID, ROW_NUMBER() OVER (ORDER BY ID) AS row_num
    FROM F_S_Claim
)
UPDATE cte SET ID = row_num;



SELECT top 100 * FROM cj_fault_serve.dbo.F_S_Claim



-- 清理
Select * into F_Serve
FROM F_S_Claim WHERE F_engType IS  NOT  NULL  AND  F_engType != ''  AND  F_optName IS  NOT  NULL  AND  F_optName != ''  AND F_FaultDesc IS NOT NULL AND  F_FaultDesc != ''  AND B_ChassisCode IS NOT NULL AND  B_ChassisCode != '' AND F_BuyTime IS NOT NULL AND  F_BuyTime != '' AND F_BuyTime IS NOT NULL AND  F_BuyTime != ''


-- 加故障件
SELECT y.P_PartName,y.F_Mid_ClaimId INTO B
FROM S_Claim x join S_ClaimParts y 
on x.F_PKId = y.F_Mid_ClaimId


UPDATE F_S_Claim
SET P_PartName = B.P_PartName
FROM F_S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId


SELECT B.P_PartName,x.F_ClaimCode 
FROM F_S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId


-- 加故障件code
SELECT y.P_PartCode,y.P_PartName,y.F_Mid_ClaimId INTO B
FROM F_S_Claim x join F_S_ClaimParts y 
on x.F_PKId = y.F_Mid_ClaimId


UPDATE F_S_Claim
SET P_PartCode = B.P_PartCode
FROM F_S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId


SELECT B.P_PartName,x.F_ClaimCode 
FROM F_S_Claim x join B
on x.F_PKId = B.F_Mid_ClaimId
-- 现在我的表S_Claim的列ID的所有值都是1，怎样把ID列变成1，2，3，4.....这样


--加入score，设置默认值为 3
ALTER TABLE F_S_Claim ADD score INT DEFAULT 3;
UPDATE F_S_Claim SET score = 3;


-- 给id创建索引，加速
CREATE INDEX idx_F_S_Claim_id ON F_S_Claim (id);



-- 一般查询

select COUNT(*) from F_S_Claim 

select COUNT(*) from F_Serve fs  

SELECT top 11 * FROM cj_fault_serve.dbo.F_Serve



-- 给id创建索引，加速
CREATE INDEX idx_F_S_Claim_id ON F_S_Claim (id);



--有多少
SELECT COUNT(*)  FROM cj_fault_serve.dbo.F_S_ClaimParts x
WHERE P_PartSpec != ''

SELECT DISTINCT (P_PartSpec)  FROM cj_fault_serve.dbo.F_S_ClaimParts x
WHERE P_PartSpec != ''


-- 查找cj_fault_serve.dbo.F_S_ClaimParts表中字段P_PartSpec的数量前8的P_PartSpec和其数量
SELECT TOP 20 P_PartSpec, COUNT(*) AS PartSpecCount
FROM cj_fault_serve.dbo.F_S_ClaimParts
GROUP BY P_PartSpec
ORDER BY PartSpecCount DESC;

-- 查找cj_fault_serve.dbo.F_S_ClaimParts表中字段P_PartSpec的值是’国四莱动4L18CF‘的不同的P_PartName和其数量
SELECT P_PartName, COUNT(*) AS PartNameCount
FROM cj_fault_serve.dbo.F_S_ClaimParts
WHERE P_PartType = N'DE4E9A34-230F-45CC-AD68-864C7636C32D'
GROUP BY P_PartName;
ORDER BY PartNameCount DESC;


SELECT COUNT(*) from F_S_ClaimParts where P_PartSpec like N'DX%'



-- 查找cj_fault_serve.dbo.F_S_ClaimParts表中字段P_PartType的数量前8的P_PartSpec和其数量
SELECT TOP 20 P_PartType, COUNT(*) AS PartSpecCount
FROM cj_fault_serve.dbo.F_S_ClaimParts
GROUP BY P_PartType
ORDER BY PartSpecCount DESC;

```

